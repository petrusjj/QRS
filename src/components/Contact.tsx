type Props = {};

const VCards = (props: Props) => {
  return (
    <div className="h-screen flex grow items-center justify-center">
      <button className="h-10 bg-red-500">Download contact</button>
    </div>
  );
};

export default VCards;
